## Setup
1. Open the project in XCode and resolve signing issues (renaming package, etc.)
2. Install Pod dependencies ```pod install```
3. Launch application on the emulator or device


Note: There must be events and a user for the application to work correctly.
